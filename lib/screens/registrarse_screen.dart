import 'package:flutter/material.dart';

class RegistrarseScreen extends StatelessWidget {
  const RegistrarseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Registrarse")));
  }
}
